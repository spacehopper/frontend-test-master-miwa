/* import { Injectable } from '@angular/core';
import { FormControl, AsyncValidator, ValidationErrors } from '@angular/forms';
import { Observable, of } from 'rxjs';
import { map, catchError } from 'rxjs/operators';

import { BeerStoreService } from '../../../shared/beer-store.service';

@Injectable({
  providedIn: 'root'
})
export class BeerExistsValidatorService implements AsyncValidator {

  constructor(private bs: BeerStoreService) { }

}
 */